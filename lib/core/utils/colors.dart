import 'package:flutter/material.dart';

/// Shared color tokens for the DataPulse visual system.
abstract final class AppColors {
  static const background = Color(0xFF080B10);
  static const surface = Color(0xFF10161D);
  static const card = Color(0xFF151D25);
  static const cardElevated = Color(0xFF1A242E);
  static const border = Color(0xFF26323D);

  static const primary = Color(0xFF168BFF);
  static const primaryDark = Color(0xFF075CA8);
  static const accent = Color(0xFF00D7D9);

  static const textPrimary = Color(0xFFF3F6F8);
  static const textSecondary = Color(0xFF8B96A3);
  static const textDisabled = Color(0xFF53606D);

  static const priorityHigh = Color(0xFFFF5A68);
  static const priorityMedium = Color(0xFF168BFF);
  static const priorityLow = Color(0xFF687687);
  static const success = Color(0xFF2DD4A8);
}
